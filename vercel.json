{
  "buildCommand": "prisma generate && next build && node scripts/copy-prisma-engines.js",
  "framework": "nextjs",
  "regions": ["iad1"],
  "build": {
    "env": {
      "PRISMA_CLI_BINARY_TARGETS": "native,rhel-openssl-3.0.x,debian-openssl-3.0.x,linux-musl-openssl-3.0.x"
    }
  }
} 